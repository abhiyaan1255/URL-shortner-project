<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/login.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
     <%- include('../partials/header') %>
    <h1>Edit password page</h1>
    <div class="container">
        <form action="/edit-password" method="post" class="Shortner_form">
             <div class="inpute_div">
            <label for="old_password">old password</label>
            <input type="password" name="oldPassword" id="old_password">
        </div>
            <div class="inpute_div">
        <label for="password">New password</label>
        <input type="password" name="newPassword" id="password">
        </div>
         <div class="inpute_div">
        <label for="confirmPassword">Confirm password</label>
        <input type="password" name="confirmPassword" id="password">
         </div>
          <span>
                <%if(errors && errors.length>0){%>
                    <%errors.forEach((error)=>{%>
                   <p class="erorr_class"> <%=error%></p>
                   <%})%>
                <%}%>
            </span>
            <span id="button_span">
        <button id="Shortner_button" type="submit">Update password</button>
        </span>
        </form>
    </div>
</body>
</html>